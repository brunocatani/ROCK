// resources.h — Resource IDs for the ROCK DLL.
// ROCK embeds its default ROCK.ini so ConfigBase can extract it on first run.
// Currently minimal — ROCK reads INI from disk via CSimpleIniA, so no embedded
// resources are strictly needed. This file exists to satisfy the resources.rc template.

#define IDR_ROCK_INI 101
