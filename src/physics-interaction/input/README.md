# Input Domain

This module owns controller input remapping and menu/config gating for interaction buttons. It is intentionally separate from hand/grab behavior so input edge capture can be tested without pulling in Havok or generated body state.
