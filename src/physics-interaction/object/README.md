# Object Domain

This module owns object detection, body-set scanning, body classification, geometry-body resolution, and external provider body registration. It exists to keep object identity and candidate-body policy out of hand/weapon code while still routing native body reads through the shared runtime boundary.
